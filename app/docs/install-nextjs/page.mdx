export const metadata = {
  title: 'Install Next.js - Bundui',
  description:
    'Bundui components were created for React. So you need to set up a React project. In this case, we can recommend you to install Next.js project.'
};

# Install Next.js

Install Next.js with Create Next App

## Create a new project

<pre>
  <code>npx create-next-app@latest</code>
</pre>

## Start the app

<pre>
  <code>cd my-app npm run dev</code>
</pre>
