export const metadata = {
  title: "Button - Bundui",
  description:
    "An animated button component to enhance your app's user experience and interactivity.",
};

import BaseButtonExample from "@/components/examples/button/base";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs"

# Button

Button component for redirects and forms.

<ComponentCodePreview
  classNameComponentContainer="min-h-0 py-24 lg:px-32 px-8"
  component={<BaseButtonExample />}
  filePath="examples/button/base"
/>

## Installation

<Tabs defaultValue="manuel" className="w-[400px]">
    <TabsList>
        <TabsTrigger value="manuel">Manuel</TabsTrigger>
        <TabsTrigger disabled value="cli">CLI (Coming Soon)</TabsTrigger>
    </TabsList>
</Tabs>

### Install the following dependencies:

<CodeRenderer code="npm i @radix-ui/react-slot class-variance-authority" lang="bash" />### Add util file

<CodeBlock filePath="lib/utils.ts" />

### Copy and paste the following code into your project.

<CodeBlock filePath="components/ui/button.tsx" />


## API

<a href="https://ui.shadcn.com/docs/components/button" target="_blank">Visit the official page</a> for more options.
