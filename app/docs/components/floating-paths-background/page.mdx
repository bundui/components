export const metadata = {
    title: "Floating Paths Background - Bundui",
    description:
        "Floating Paths Background is a decorative background animation component that creates soft, looping path shapes floating across the screen.",
};

import FloatingPathsBackgroundExample from "@/components/examples/backgrounds/floating-paths";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs"

# Floating Paths Background

Floating Paths Background is a decorative background animation component that creates soft, looping path shapes floating across the screen. It uses SVG and motion-based techniques to add visual interest without overwhelming the content. Ideal for hero sections, landing pages, and minimal UI designs. Built with Tailwind CSS and Framer Motion for smooth performance and responsiveness.

<ComponentCodePreview
    component={<FloatingPathsBackgroundExample/>}
    filePath="examples/backgrounds/floating-paths"
/>

## Installation

<Tabs defaultValue="manual" className="w-[400px]">
    <TabsList>
        <TabsTrigger value="manual">Manual</TabsTrigger>
        <TabsTrigger disabled value="cli">CLI (Coming Soon)</TabsTrigger>
    </TabsList>
</Tabs>

### Install the following dependencies:

<CodeRenderer code={`npm i motion clsx tailwind-merge`} lang="bash"/>

### Add utils file

<CodeBlock filePath="lib/utils.ts" />

### Copy and paste the following code into your project:

<CodeBlock filePath="components/core/backgrounds/floating-paths.tsx" />

## API

| Prop        | Type              | Default | Description                                                                 |
|-------------|-------------------|---------|-----------------------------------------------------------------------------|
| `position`  | `number`          |         | A multiplier used to calculate the offset and curve of each path.          |
| `children`  | `React.ReactNode` |         | The content that will be rendered above the floating background paths.     |
| `className` | `string`          | `undefined`    | Optional Tailwind/CSS classes for custom styling on the outer container.   |
