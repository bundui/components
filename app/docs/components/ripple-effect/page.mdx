export const metadata = {
  title: "Ripple Effect - Bundui",
  description:
    "Ripple effect is a modern animation component designed to enrich user interaction by creating a dynamic ripple effect when clicking on buttons or any HTML element. Built with Tailwind CSS and Motion.",
};

import { RippleEffectButton } from "@/components/examples/ripple-effect/button";
import { RippleEffectImage } from "@/components/examples/ripple-effect/image";
import { RippleEffectColored } from "@/components/examples/ripple-effect/colored";

# Ripple Effect

Ripple effect is a modern animation component designed to enrich user interaction by creating a dynamic ripple effect when clicking on buttons or any HTML element. This component generates a visual ripple effect, making the user experience more interactive and visually appealing. Perfect for modern web designs.

## Button

<ComponentCodePreview
  classNameComponentContainer="min-h-0 py-24 lg:px-32 px-8"
  component={<RippleEffectButton />}
  filePath="examples/ripple-effect/button"
/>

## Image

<ComponentCodePreview
  classNameComponentContainer="min-h-0 py-24 lg:px-32 px-8"
  component={<RippleEffectImage />}
  filePath="examples/ripple-effect/image"
/>

## Colored

<ComponentCodePreview
  classNameComponentContainer="min-h-0 py-24 lg:px-32 px-8"
  component={<RippleEffectColored />}
  filePath="examples/ripple-effect/colored"
/>

### Install the following dependencies:

<CodeRenderer code="npm i motion clsx tailwind-merge" lang="bash" />

### Add util file

<CodeBlock filePath="lib/utils.ts" />

### Copy and paste the following code into your project.

<CodeBlock filePath="components/core/ripple-effect.tsx" />

## API

| Prop              | Type                                                | Default      | Description                                                                 |
|-------------------|-----------------------------------------------------|--------------|-----------------------------------------------------------------------------|
| children          | `React.ReactNode`                                   |              | The content to be wrapped with the ripple effect                           |
| color             | `string`                                            | `"rgba(255, 255, 255, 0.7)"` | The color of the ripple effect                                             |
| duration          | `number`                                            | `0.8`        | The duration (in seconds) for which the ripple effect will expand and fade   |
| className         | `string`                                            |              | Custom className for the container of the ripple effect                    |

