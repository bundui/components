export const metadata = {
  title: "Text Gradient Scroll - Bundui",
  description:
    "Discover the allure of animated gradient text, a dynamic UI component that enhances user engagement with smooth color transitions. Built with Tailwind CSS and Motion.",
};

import TextGradientScrollExample from "@/components/examples/text-gradient-scroll";

# Text Gradient Scroll

The text gradient scroll component is designed to enhance user interaction by providing a visually dynamic effect as the user scrolls through the text. Unlike static text, this effect offers a more engaging visual experience with smooth color transitions that change as the text is scrolled. The animated gradient shifts add a modern and interactive touch to the user experience..

<ComponentCodePreview
  classNameComponentContainer="min-h-0 py-24 lg:px-32 px-8"
  component={<TextGradientScrollExample />}
  filePath="examples/text-gradient-scroll"
  iframeDemo="/demo/text-gradient-scroll"
/>

## Installation

<Tabs defaultValue="manuel" className="w-[400px]">
    <TabsList>
        <TabsTrigger value="manuel">Manuel</TabsTrigger>
        <TabsTrigger disabled value="cli">CLI (Coming Soon)</TabsTrigger>
    </TabsList>
</Tabs>

### Install the following dependencies:

<CodeRenderer code="npm i motion clsx tailwind-merge" lang="bash" />

### Add util file

<CodeBlock filePath="lib/utils.ts" />

### Copy and paste the following code into your project.

<CodeBlock filePath="components/core/text-gradient-scroll.tsx" />

## API

| Prop        | Type                         | Default  | Description                                                               |
| :---------- | :--------------------------- | :------- | :------------------------------------------------------------------------ |
| text        | string                       |          | The text content to display with gradient effect.                         |
| className   | string                       |          | Optional CSS class for styling the component.                             |
| type        | "letter" \| "word"           | "letter" | Determines the type of animation, whether it applies to letters or words. |
| textOpacity | "none" \| "soft" \| "medium" | "soft"   | Sets the opacity level for the text: none, soft, medium, or strong.       |
