---
title: Countdown
description: A countdown is a component that displays the remaining time until a specific event or deadline.
---

<ComponentCodePreview name="countdown"/>

## Installation

<ComponentInstallation name="countdown" deps={"npm install motion"}/>

## Usage

<CodeRenderer name="countdown"/>
## Props

<TypeTable
  type={{
    value: {
      type: 'number',
      description: 'The numeric value to display with animated sliding digits.',
    },
    padStart: {
      type: 'boolean',
      default: 'false',
      description: 'If true, prepends a zero to single-digit numbers (e.g., "07" instead of "7").',
    },
    decimalSeparator: {
      type: 'string',
      default: '"."',
      description: 'Character used to separate the integer and decimal parts of the number.',
    },
  }}
/>
